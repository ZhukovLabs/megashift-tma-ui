export const ROUTES = {
    root: '/',
    onboarding: '/onboarding',
    schedule: '/schedule',
    profile: '/profile',
    statistics: '/statistics',
    shifts: '/shifts',
    settings: '/settings',
} as const;